---
import LandingLayout from '../layouts/LandingLayout.astro';
import MainNavbar from '../components/MainNavbar.astro';
---

<LandingLayout 
  title="Contacto" 
  description="¿Tenés alguna duda sobre Turni.pro? Contactanos y te responderemos en menos de 24 horas."
>
  <MainNavbar />
  
  <main class="flex-grow">
    <!-- Hero Section -->
    <section class="hero-mesh relative pt-32 pb-24 lg:pt-40 lg:pb-32 overflow-hidden">
      <div class="absolute top-1/4 left-1/4 w-96 h-96 bg-brand-500/20 rounded-full blur-[100px] pointer-events-none"></div>
      <div class="absolute bottom-0 right-1/4 w-[500px] h-[500px] bg-blue-600/10 rounded-full blur-[120px] pointer-events-none"></div>
      
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10 text-center">
        <div class="inline-flex items-center gap-2 bg-white/5 backdrop-blur-sm px-4 py-2 rounded-full border border-white/10 mb-6">
          <span class="text-sm text-brand-300 font-medium tracking-wide font-display">Soporte y Ventas</span>
        </div>
        
        <h1 class="text-4xl sm:text-5xl lg:text-6xl font-display font-extrabold text-white leading-tight mb-6">
          Estamos para <br/>
          <span class="text-gradient">ayudarte.</span>
        </h1>
        
        <p class="text-lg text-slate-300 max-w-2xl mx-auto font-light leading-relaxed">
          ¿Tenés alguna duda sobre Turni.pro o querés saber más sobre nuestros planes? Completá el formulario y nos pondremos en contacto con vos.
        </p>
      </div>
    </section>

    <!-- Contact Form Section -->
    <section class="relative -mt-16 pb-24 z-20">
      <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="bg-white dark:bg-surface-900 p-8 md:p-10 rounded-3xl shadow-2xl border border-slate-100 dark:border-slate-800">
          <div class="mb-8">
            <h2 class="text-2xl md:text-3xl font-bold text-slate-900 dark:text-white font-display mb-2">Envianos un mensaje</h2>
            <p class="text-slate-500 dark:text-slate-400">Completá los campos y te responderemos en menos de 24 horas.</p>
          </div>
          
          <form id="contact-form" class="space-y-6" method="POST" action="/api/contact">
            <div class="grid md:grid-cols-2 gap-6">
              <div class="space-y-2">
                <label class="block text-sm font-bold text-slate-700 dark:text-slate-300" for="name">
                  Nombre completo
                </label>
                <div class="relative">
                  <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none text-slate-400">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
                    </svg>
                  </div>
                  <input 
                    class="w-full rounded-xl border-slate-200 dark:border-slate-700 bg-slate-50 dark:bg-slate-800 pl-10 pr-4 py-3.5 text-slate-900 dark:text-white placeholder-slate-400 focus:ring-2 focus:ring-brand-500 focus:border-transparent outline-none transition-all" 
                    id="name" 
                    name="name"
                    placeholder="Ej. Juan Pérez" 
                    type="text"
                    required
                  />
                </div>
              </div>
              
              <div class="space-y-2">
                <label class="block text-sm font-bold text-slate-700 dark:text-slate-300" for="email">
                  Email
                </label>
                <div class="relative">
                  <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none text-slate-400">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
                    </svg>
                  </div>
                  <input 
                    class="w-full rounded-xl border-slate-200 dark:border-slate-700 bg-slate-50 dark:bg-slate-800 pl-10 pr-4 py-3.5 text-slate-900 dark:text-white placeholder-slate-400 focus:ring-2 focus:ring-brand-500 focus:border-transparent outline-none transition-all" 
                    id="email" 
                    name="email"
                    placeholder="juan@ejemplo.com" 
                    type="email"
                    required
                  />
                </div>
              </div>
            </div>
            
            <div class="space-y-2">
              <label class="block text-sm font-bold text-slate-700 dark:text-slate-300" for="subject">
                Asunto
              </label>
              <div class="relative">
                <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none text-slate-400">
                  <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 7h.01M7 3h5c.512 0 1.024.195 1.414.586l7 7a2 2 0 010 2.828l-7 7a2 2 0 01-2.828 0l-7-7A1.994 1.994 0 013 12V7a4 4 0 014-4z" />
                  </svg>
                </div>
                <input 
                  class="w-full rounded-xl border-slate-200 dark:border-slate-700 bg-slate-50 dark:bg-slate-800 pl-10 pr-4 py-3.5 text-slate-900 dark:text-white placeholder-slate-400 focus:ring-2 focus:ring-brand-500 focus:border-transparent outline-none transition-all" 
                  id="subject" 
                  name="subject"
                  placeholder="¿En qué podemos ayudarte?" 
                  type="text"
                  required
                />
              </div>
            </div>
            
            <div class="space-y-2">
              <label class="block text-sm font-bold text-slate-700 dark:text-slate-300" for="message">
                Mensaje
              </label>
              <div class="relative">
                <textarea 
                  class="w-full rounded-xl border-slate-200 dark:border-slate-700 bg-slate-50 dark:bg-slate-800 px-4 py-3.5 text-slate-900 dark:text-white placeholder-slate-400 focus:ring-2 focus:ring-brand-500 focus:border-transparent outline-none transition-all min-h-[160px] resize-y" 
                  id="message" 
                  name="message"
                  placeholder="Escribí tu mensaje acá..."
                  required
                ></textarea>
              </div>
            </div>
            
            <div class="pt-4 flex items-center justify-between gap-4 flex-wrap">
              <p class="text-xs text-slate-500 dark:text-slate-400 max-w-xs">
                Al enviar este formulario, aceptás nuestra <a class="underline hover:text-brand-500 transition-colors" href="#">política de privacidad</a>.
              </p>
              <button 
                class="bg-brand-500 hover:bg-brand-400 text-white px-8 py-4 rounded-xl font-bold text-lg transition-all shadow-glow hover:shadow-lg hover:-translate-y-0.5 flex items-center gap-2 group w-full sm:w-auto justify-center" 
                type="submit"
              >
                Enviar mensaje
                <svg class="w-5 h-5 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 19l9 2-9-18-9 18 9-2zm0 0v-8" />
                </svg>
              </button>
            </div>
          </form>
        </div>
      </div>
    </section>
  </main>

  <!-- Footer -->
  <footer id="footer" class="bg-surface-900 border-t border-slate-800 pt-20 pb-10">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid md:grid-cols-4 gap-12 mb-16">
        <div class="col-span-1 md:col-span-2">
          <div class="flex items-center gap-3 mb-6">
            <div class="w-10 h-10 bg-brand-600 rounded-xl flex items-center justify-center text-white">
              <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
              </svg>
            </div>
            <span class="text-2xl font-display font-bold text-white tracking-tight">turni.pro</span>
          </div>
          <p class="text-slate-400 text-sm max-w-sm mb-8 leading-relaxed">
            La plataforma de gestión de turnos que tu negocio necesita. Simple, profesional y confiable.
          </p>
        </div>
        
        <div>
          <h4 class="text-white font-bold font-display mb-6">Producto</h4>
          <ul class="space-y-4 text-sm text-slate-400">
            <li><a class="hover:text-brand-400 transition-colors" href="/#como-funciona">Cómo funciona</a></li>
            <li><a class="hover:text-brand-400 transition-colors" href="/#precios">Precios</a></li>
            <li><a class="hover:text-brand-400 transition-colors" href="/registro">Empezar gratis</a></li>
          </ul>
        </div>
        
        <div>
          <h4 class="text-white font-bold font-display mb-6">Cuenta</h4>
          <ul class="space-y-4 text-sm text-slate-400">
            <li><a class="hover:text-brand-400 transition-colors" href="/login">Iniciar sesión</a></li>
            <li><a class="hover:text-brand-400 transition-colors" href="/registro">Crear cuenta</a></li>
          </ul>
        </div>
      </div>
      
      <div class="border-t border-slate-800 pt-8 flex flex-col md:flex-row justify-between items-center text-xs text-slate-500">
        <p>© 2024 turni.pro. Todos los derechos reservados.</p>
        <div class="flex space-x-8 mt-4 md:mt-0">
          <a class="hover:text-slate-300 transition-colors" href="#">Privacidad</a>
          <a class="hover:text-slate-300 transition-colors" href="#">Términos</a>
        </div>
      </div>
    </div>
  </footer>
</LandingLayout>

<style>
  .hero-mesh {
    background-color: #0f172a;
    background-image: 
      radial-gradient(at 0% 0%, hsla(168,76%,45%,0.15) 0px, transparent 50%),
      radial-gradient(at 100% 0%, hsla(168,76%,45%,0.1) 0px, transparent 50%),
      radial-gradient(at 100% 100%, hsla(220,30%,20%,1) 0px, transparent 50%);
  }
  
  .text-gradient {
    background: linear-gradient(135deg, #2dd4bf 0%, #38ef7d 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }
  
  .shadow-glow {
    box-shadow: 0 0 20px rgba(20, 184, 166, 0.3);
  }
</style>

<script>
  const form = document.getElementById('contact-form');
  const submitButton = form?.querySelector('button[type="submit"]');
  
  if (form && submitButton) {
    form.addEventListener('submit', async (e) => {
      e.preventDefault();
      
      const originalText = submitButton.textContent;
      submitButton.disabled = true;
      submitButton.textContent = 'Enviando...';
      
      const formData = new FormData(form);
      
      try {
        const response = await fetch('/api/contact', {
          method: 'POST',
          body: formData,
        });
        
        const data = await response.json();
        
        if (data.success) {
          // Mostrar mensaje de éxito
          const successMessage = document.createElement('div');
          successMessage.className = 'bg-green-50 dark:bg-green-900/20 border border-green-200 dark:border-green-800 text-green-800 dark:text-green-200 px-6 py-4 rounded-xl mb-6';
          successMessage.innerHTML = `
            <div class="flex items-center gap-3">
              <svg class="w-5 h-5 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
              </svg>
              <p class="font-medium">${data.message || 'Mensaje enviado correctamente. Te responderemos en menos de 24 horas.'}</p>
            </div>
          `;
          
          form.insertBefore(successMessage, form.firstChild);
          form.reset();
          
          // Scroll a la parte superior del formulario
          successMessage.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
          
          // Remover mensaje después de 5 segundos
          setTimeout(() => {
            successMessage.remove();
          }, 5000);
        } else {
          // Mostrar mensaje de error
          const errorMessage = document.createElement('div');
          errorMessage.className = 'bg-red-50 dark:bg-red-900/20 border border-red-200 dark:border-red-800 text-red-800 dark:text-red-200 px-6 py-4 rounded-xl mb-6';
          errorMessage.innerHTML = `
            <div class="flex items-center gap-3">
              <svg class="w-5 h-5 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd" />
              </svg>
              <p class="font-medium">${data.error || 'Error al enviar el mensaje. Por favor intentá nuevamente.'}</p>
            </div>
          `;
          
          form.insertBefore(errorMessage, form.firstChild);
          
          // Scroll a la parte superior del formulario
          errorMessage.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
          
          // Remover mensaje después de 5 segundos
          setTimeout(() => {
            errorMessage.remove();
          }, 5000);
        }
      } catch (error) {
        console.error('Error:', error);
        const errorMessage = document.createElement('div');
        errorMessage.className = 'bg-red-50 dark:bg-red-900/20 border border-red-200 dark:border-red-800 text-red-800 dark:text-red-200 px-6 py-4 rounded-xl mb-6';
        errorMessage.innerHTML = `
          <div class="flex items-center gap-3">
            <svg class="w-5 h-5 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd" />
            </svg>
            <p class="font-medium">Error de conexión. Por favor intentá nuevamente.</p>
          </div>
        `;
        
        form.insertBefore(errorMessage, form.firstChild);
        errorMessage.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
        
        setTimeout(() => {
          errorMessage.remove();
        }, 5000);
      } finally {
        submitButton.disabled = false;
        submitButton.textContent = originalText;
      }
    });
  }
</script>
